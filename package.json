{
  "name": "pulse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --env-file-if-exists=.env src/pulse.mjs"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
